# Overview

A small PoC to build a simple User API service that demonstrates a full SDLC workflow (outer-loop → inner-loop) using Claude and OpenCode across planning, development, infrastructure, and CI/CD.

The system will expose a clean API and deploy automatically to Azure using Terraform and GitHub Actions.
